import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputContainer: {
    borderWidth: 1,
    width: '100%',
    marginTop: 30,
    borderRadius: 12,
  },
  input: {
      paddingStart:'5%',
      fontSize: 13
  },
});

export default styles;
